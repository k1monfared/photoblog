#!/bin/bash
output=$(python process_photos.py 2>&1)
if echo "$output" | grep -q "Processed [1-9]"; then
    echo "$output"
    git add metadata/ files/photoblog/ *.md
    echo "New photos processed and staged. Review then re-commit."
    exit 1
fi
